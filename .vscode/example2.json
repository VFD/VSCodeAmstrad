{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Script SH",
            "type": "shell",
            "command": "./monscript.sh",
            "args": ["${file}", "${fileBasenameNoExtension}", "${relativeFileDirname}"],
            "problemMatcher": [],
            "enabled": "${env:ENABLE_SH}"
        },
        {
            "label": "Script PS1",
            "type": "shell",
            "command": "./monscript.ps1",
            "args": ["${file}", "${fileBasenameNoExtension}", "${relativeFileDirname}"],
            "problemMatcher": [],
            "enabled": "${env:ENABLE_PS1}"
        },
        {
            "label": "Script CMD",
            "type": "shell",
            "command": "./monscript.cmd",
            "args": ["${file}", "${fileBasenameNoExtension}", "${relativeFileDirname}"],
            "problemMatcher": [],
            "enabled": "${env:ENABLE_CMD}"
        }
    ]
}
